import XCTest
@testable import wishkit_ios_shared

final class wishkit_ios_sharedTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(wishkit_ios_shared().text, "Hello, World!")
    }
}
